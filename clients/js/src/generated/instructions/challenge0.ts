/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  publicKey,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findTreasuryPda } from '../../hooked';
import { findProofPda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type Challenge0InstructionAccounts = {
  /** The Core asset to burn */
  asset: PublicKey | Pda;
  /** The collection the asset belongs to (if any) */
  collection?: PublicKey | Pda;
  /** The owner of the asset (receives SOL reward) */
  owner: Signer;
  /** The proof of challenge 0 completion */
  proof?: PublicKey | Pda;
  /** The treasury PDA that holds SOL rewards */
  treasury?: PublicKey | Pda;
  /** The MPL Core program */
  mplCoreProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type Challenge0InstructionData = { discriminator: number };

export type Challenge0InstructionDataArgs = {};

export function getChallenge0InstructionDataSerializer(): Serializer<
  Challenge0InstructionDataArgs,
  Challenge0InstructionData
> {
  return mapSerializer<
    Challenge0InstructionDataArgs,
    any,
    Challenge0InstructionData
  >(
    struct<Challenge0InstructionData>([['discriminator', u8()]], {
      description: 'Challenge0InstructionData',
    }),
    (value) => ({ ...value, discriminator: 0 })
  ) as Serializer<Challenge0InstructionDataArgs, Challenge0InstructionData>;
}

// Instruction.
export function challenge0(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: Challenge0InstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'mplFaucet',
    'FAUCp7gqwz4tv1XpxqBQ3J9p8kVATc2m1bvMvHvhg9A3'
  );

  // Accounts.
  const resolvedAccounts = {
    asset: {
      index: 0,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    collection: {
      index: 1,
      isWritable: true as boolean,
      value: input.collection ?? null,
    },
    owner: {
      index: 2,
      isWritable: true as boolean,
      value: input.owner ?? null,
    },
    proof: {
      index: 3,
      isWritable: true as boolean,
      value: input.proof ?? null,
    },
    treasury: {
      index: 4,
      isWritable: true as boolean,
      value: input.treasury ?? null,
    },
    mplCoreProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.proof.value) {
    resolvedAccounts.proof.value = findProofPda(context, {
      challenge: 0,
      owner: expectPublicKey(resolvedAccounts.owner.value),
    });
  }
  if (!resolvedAccounts.treasury.value) {
    resolvedAccounts.treasury.value = findTreasuryPda(context);
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = publicKey(
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getChallenge0InstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
